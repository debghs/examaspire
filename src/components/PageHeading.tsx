export function PageSubHeading({ children }: { children: React.ReactNode }) {
  return (
    <h2 className="text-[#201A09] text-[22px] font-bold leading-tight tracking-[-0.015em] pb-3 pt-5">
      {children}
    </h2>
  )
}
